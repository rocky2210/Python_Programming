# os.path.isfile(path): Returns True if the path points to a file.

import os
if os.path.isfile("/Python_programming/Python_programs/14_Libraries/os/notes.txt"):
    print("It's a file")
else:
    print("It's not a file")
