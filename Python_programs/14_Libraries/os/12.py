# os.path.isdir(path): Returns True if the path points to a directory.

import os
if os.path.isdir("/Python_programming/Python_programs/14_Libraries/os"):
    print("It's a directory")
else:
    print("It's not a directory")
